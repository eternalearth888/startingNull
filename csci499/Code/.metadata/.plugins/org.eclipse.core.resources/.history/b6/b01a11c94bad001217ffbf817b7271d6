package gui;

import javax.swing.*;

public class InstructionPanel extends JFrame {
	setSize(new Dimenson(400,400));
	setResizable(false);
	setTitle("Instructions");
	
	JTextArea instructions = new JTextArea();
	
	Scanner read;
	String addLine = "";
	
	try {
		read = new Scanner(new File("textFiles/instructions"))
	}
}
